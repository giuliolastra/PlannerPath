//Query per vedere quale vertice è il più profondo

//Query per contare attributi di un vertice (si può usare l'uid del vertice più profondo e si evita il primo passo

// Ottieni edge collegato

// Salvare elenco attributi con resultCollector

//ciclo for in cui da depth + alta a depth + bassa
for($i = 0;$i < mod($depthA - $depthB); $i++) {
	
	//Fai query per attributi vertice

	//Foreach per inserimento in PathResult con addAttribute(Nome, Valore);

	//addVertexName() per aggiungere il nome vertice

	// Foreach per edge come sopra

}